{
  "mcpServers": {
    "github": {
      "command": "uvx",
      "args": [
        "--with", "requests",
        "--with", "python-dotenv",
        "fastmcp",
        "run",
        "${CLAUDE_PLUGIN_ROOT}/server.py"
      ],
      "env": {
        "GITHUB_TOKEN": "${GH_TOKEN}"
      }
    }
  }
}
