{
  "mcpServers": {
    "github": {
      "command": "uvx",
      "args": [
        "--with", "requests",
        "--with", "python-dotenv",
        "fastmcp",
        "run",
        "./server.py"
      ]
    }
  }
}
