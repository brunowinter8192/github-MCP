{
  "mcpServers": {
    "github": {
      "command": "uvx",
      "args": [
        "--with", "requests",
        "fastmcp",
        "run",
        "${CLAUDE_PLUGIN_ROOT}/server.py"
      ],
      "env": {
        "GITHUB_TOKEN": "${GH_TOKEN}"
      }
    }
  }
}
