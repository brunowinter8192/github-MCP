# Class F + G Re-Clean Apply (2026-09-05)

Process record for the one and only `--apply` run of `dev/content_cleaning/07_reclean_migration_headers.py`
against the live `github_issues` corpus, applying the combined class-F (tracker-migration header +
rule) and class-G (automated version-removal comment) strip approved earlier this same session. No
reindex was run as part of this work — that step is out of scope here and is being done and
recorded separately. Follows this area's earlier apply entry for shape.

## File apply — before/after, parallel-write check

Recorded immediately before: **963 files**, newest mtime `2026-09-05T15:33:13.850330`
(`searxng__1003.md`).

`07_reclean_migration_headers.py --apply` ran. Backup created at
`data/documents/github_issues_PRE_MIGRATION_NOISE_STRIP_BACKUP_20260905_190957` — confirmed to
hold the full pre-strip corpus (963 files) before any file was overwritten, and confirmed to still
contain the original class-F/G text for `pyobjc__175.md` (4 matches of the header/marker patterns,
vs. 0 in the live corpus after the apply). This backup is deliberately not a disposable safety net
and must not be deleted.

Recorded immediately after: **963 files** (unchanged — the re-clean only overwrites, never
adds/removes), newest mtime `2026-09-05T19:09:58.230296` (`pyobjc__77.md`). Every file with an
mtime at or after the apply's write-window start (`>= 2026-09-05T19:09:58`) was enumerated: exactly
7 — `pyobjc__175.md`, `pyobjc__176.md`, `pyobjc__31.md`, `pyobjc__34.md`, `pyobjc__44.md`,
`pyobjc__76.md`, `pyobjc__77.md` — a direct set comparison against the expected 7 filenames showed
an exact match, no extra file, no missing file. `pyobjc__610.md` (the native, non-migrated pyobjc
issue) confirmed absent from the rewritten set, as expected. The 963 `.json` sidecars in the same
directory were untouched by this apply (regenerated by `rag-cli index`, not written by this
script), and no reindex was triggered.

## Numbers: apply run vs. the approved dry-run

The apply run's own report (`dev/content_cleaning/md/07_reclean_dryrun_20260905_190957.md`) was
diffed line-for-line against the approved dry-run report from earlier this session
(`07_reclean_dryrun_20260905_190609.md`): identical apart from the header timestamp line. Both
report:

- Files changed: **7 / 963**.
- Spans: **36 total** (**31 class F**, **5 class G**).
- Lines removed: **131 total** (**93 F**, **38 G**).
- Chars removed: **7,942 total** (**6,568 F**, **1,374 G**).

Since `measure_all()` is recomputed fresh on every invocation (not read from a stored value), an
identical result between the two runs is itself evidence that nothing in the corpus changed
between the approval and the apply, and that the apply overwrote exactly what was measured and
approved — no drift between what was reviewed and what was written.

## Result

The corpus's first class-F/G strip is live: 7 files changed, 36 spans (31 F + 5 G), 131 lines,
7,942 chars removed from the corpus, matching the approved dry-run exactly. Backup preserved at
`github_issues_PRE_MIGRATION_NOISE_STRIP_BACKUP_20260905_190957` as the sole remaining record of
the pre-strip corpus for these 7 files (they have no earlier raw-fetch counterpart under
`logs/raw_issues/`, since raw logging only covers fetches from the class-F/G work's own session
onward — see this area's earlier entries). The RAG index has not been touched by this work; the
reindex and its resulting chunk-count delta are recorded separately.
